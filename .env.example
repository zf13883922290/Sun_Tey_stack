# ══════════════════════════════════════════════════════════════════
# Sun_Tey Stack — 环境变量配置
# 
# 1. 复制此文件: cp .env.example .env
# 2. 运行 GPU UUID 查询: bash scripts/01_get_gpu_uuids.sh
# 3. 填入 NGC_API_KEY（在 https://ngc.nvidia.com/ 获取）
# ══════════════════════════════════════════════════════════════════

# ── GPU UUID（由 scripts/01_get_gpu_uuids.sh 自动填写）──────────
GPU_UUID_0=GPU-xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
GPU_UUID_1=GPU-xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# ── NVIDIA NGC API Key（必填，用于拉取 NIM 容器）────────────────
# 获取方式: https://ngc.nvidia.com/ → Profile → Setup → API Key
NGC_API_KEY=

# ── NIM 模型选择 ─────────────────────────────────────────────────
# 可选: meta/llama-3.1-8b-instruct, meta/llama-3.1-70b-instruct
#       nvidia/nemotron-mini-4b-instruct, meta/codellama-13b-instruct
NIM_MODEL=meta/llama-3.1-8b-instruct

# ── 服务端口（通常不需要修改）──────────────────────────────────
NIM_PORT=8000
MCP_PORT=9000
CHROMA_PORT=8001
CUSTOMIZER_PORT=8080
DATA_DESIGNER_PORT=8081
EVALUATOR_PORT=8082
